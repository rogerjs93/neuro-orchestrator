# Development / test dependencies (runtime deps are in requirements.txt).
# Run the suite from the project root:  python -m pytest
-r requirements.txt
pytest>=8.0
httpx>=0.27        # required by fastapi.testclient
